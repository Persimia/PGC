# Box services (Phase 4 — placeholder)

Will contain: mavlink-router config, MediaMTX config, Box API (FastAPI, imports
`mission_engine`), HA-bridge. Rules that already apply (design doc §3.4):
FC1 API-first, FC2 auth+TLS day one, FC4 local SQLite flight/mission logs.
